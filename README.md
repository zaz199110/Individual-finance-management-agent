# 智能投顾助手

> 一款面向 C 端投资者的 AI 基金理财助手，专注基金研究、资产配置、持仓诊断三大投前场景，通过自由问答→需求梳理→资产配置→持仓分析→基金解读五条功能闭环覆盖完整投资决策链，支持输出留痕报告。

---

## 产品定位

一款面向 C 端投资者的 AI 基金理财助手，专注基金研究、资产配置、持仓诊断三大投前场景，通过自由问答→需求梳理→资产配置→持仓分析→基金解读五条功能闭环覆盖完整投资决策链，支持输出留痕报告。

---

## 产品亮点

### 1. 多场景 Agent 架构

由 Planner 做意图识别，将用户任务引导至 5 个场景 Subagent 执行预设 Workflow，修改类操作嵌入人工确认节点，通过工作流锁防止并发冲突。前端 SSE 分阶段推送进度，改善等待体验。

### 2. RAG 混合检索实践

以基金解读场景落地 RAG。数据层分了四级：

| 层级 | 数据源 | 说明 |
|------|--------|------|
| L0 | 实时行情 | Tushare/Akshare 获取基金净值、涨跌、规模等基础数据 |
| L1 | 结构化知识库 | 基金招募书/报告按章节分档建索引，9 个预定义查询保底 |
| L2 | 语义向量库 | FAQ 处理口语化提问，关键词反向增强 L1 检索 |
| L3 | 联网搜索 | 系统按规则验证后入库，公开资讯补充验证 |

语义匹配结果反向映射为关键词补充检索 L1，提高问答命中率。

### 3. 长对话上下文管理

多轮对话下通过分层压缩（新消息全文保留→旧消息摘要→最早消息仅留标记）控制 Token 膨胀。每次 LLM 调用前从 DB 恢复任务状态等业务数据，确保模型不因历史裁剪丢失关键上下文。

---

## 产品功能

五个功能形成闭环，覆盖五个投资场景。

### 核心功能

| 功能 | 说明 |
|------|------|
| **自由问答** | 想知道什么直接问，支持联网搜索和截图提问 |
| **需求梳理** | 确定你的风险偏好和财务目标，生成投资需求报告 |
| **资产配置** | 基于需求报告，两步生成方案：先定大类资产比例，再选具体基金 |
| **持仓诊断** | 录入现有持仓，系统自动汇总分析，生成持仓诊断报告 |
| **基金解读** | 输入基金代码，获得从行情到深层语义的多层次分析 |

### 投资场景

- 结婚生育
- 购房置业
- 子女教育
- 退休养老
- 财富增值

### 输出报告

四类报告经历「草稿 → 核验 → 确认 → 发布归档」完整流程，可回顾、可归档。

---

## 系统架构

```
用户消息 → 意图分析 → 场景分发 → 业务数据调用 → 模型推理 → 报告输出
```

**四层结构：**

1. **场景模块** — 自由问答 / 需求梳理 / 资产配置 / 持仓诊断 / 基金解读
2. **业务数据** — 用户画像、资产配置方案、持仓快照、知识库
3. **基础设施** — 多模型槽（推理/深度推理/视觉/联网/向量）、Supabase 数据库
4. **输出报告** — 投资需求报告、资产配置方案、持仓分析报告、基金解读报告

---

## 部署指南

### 环境要求

| 依赖 | 最低版本 | 说明 |
|------|----------|------|
| Node.js | 18+（推荐 20+） | 运行环境 |
| npm | 9+ | 包管理器 |
| Docker Desktop | 最新稳定版 | 仅本地演示模式需要 |
| Python | 3.9+ | 可选，高级功能需要 |

### 部署模式

| 模式 | 适用场景 | 需要 Docker | 上手时间 |
|------|----------|-------------|----------|
| 本地演示 | 本机演示、产品验收 | 是 | 约 5-10 分钟 |
| 云端数据库 | 已有 Supabase 云端项目 | 否 | 约 3-5 分钟 |

### 快速开始

```bash
# 1. 复制配置文件
cp .env.example .env.local
# 编辑 .env.local 填入你的 API 密钥

# 2. 安装依赖
npm install --legacy-peer-deps

# 3. 启动数据库（本地演示模式，需 Docker）
npm run supabase:start

# 4. 同步设置
npm run supabase:sync-settings

# 5. 初始化数据
npm run data:init

# 6. 启动开发服务器
npm run dev
```

应用启动后访问 http://localhost:3000

### 环境变量配置

在 `.env.local` 中配置以下变量：

```bash
# Supabase 配置
SUPABASE_URL=http://127.0.0.1:54321  # 本地演示
# SUPABASE_URL=https://your-project.supabase.co  # 云端
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# 大语言模型
LLM_API_KEY=your-deepseek-key
LLM_MODEL_NAME=deepseek-chat

# 应用配置
APP_ENV=development
APP_SECRET=change-me-to-a-random-string
```

详细配置说明见 [.env.example](.env.example)

---

## 技术栈

| 类别 | 技术 |
|------|------|
| 框架 | Next.js 15 + React 19 |
| 数据库 | Supabase (PostgreSQL + pgvector) |
| 样式 | Tailwind CSS 4 |
| 图表 | ECharts |
| 测试 | Vitest + Playwright |
| 类型 | TypeScript 5.8 |

---

## 开发命令

| 命令 | 用途 |
|------|------|
| `npm run dev` | 启动开发服务器 |
| `npm run build` | 生产构建 |
| `npm test` | 运行单元测试 |
| `npm run test:e2e` | 运行端到端测试 |


